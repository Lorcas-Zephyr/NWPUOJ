<% this.title = title || '操作成功' %>
<% include app_header %>
<%
const appSuccessNextUrls = typeof nextUrls !== 'undefined' && nextUrls ? nextUrls : {};
const appSuccessActions = Object.keys(appSuccessNextUrls).map((label, index) => ({ label, href: appSuccessNextUrls[label], primary: index === 0 }));
const appPageState = { type: 'success', title: message, details: typeof details !== 'undefined' ? details : '', actions: appSuccessActions, historyBack: true };
%>

<div class="app-result-page"><% include app_page_state %></div>

<% include app_footer %>
