# 后台管理优化建议

## 已完成

- 增加统一的后台用户管理入口，支持用户名、邮箱和 UID 搜索。
- 用户列表增加分页，避免一次加载全部用户。
- 列表展示邮箱验证、管理员、隐藏账户、作弊者、权限和 Rating 状态。
- 用户管理入口复用现有资料编辑权限边界，站长账户保护规则保持不变。
- 用户删除会把提交、题目、比赛、文章、题解和审计内容转移到不可登录的“账户已删除”系统账号。
- `manage_user` 可修正用户实名资料；权限矩阵已覆盖题目、题目标签、比赛和用户管理权限。
- 后台菜单统一使用明确的功能名称，Rating 页面保持只读自动结算说明。
- 比赛题目选择器会排除未启用比赛提交的 VJudge 题目，并由服务端阻止绕过页面加入。
- 后台首页集中展示待审核题解、未结工单、等待与异常评测、VJudge 配置状态和近期评测管理操作。

## 建议继续优化

- 操作审计：记录修改权限、重置密码、头像变更和作弊标记的操作者、时间、原因与前后值。
- 批量操作：为用户列表增加批量隐藏、邮箱验证状态处理和权限授予，但必须二次确认并限制危险权限。
- 权限矩阵：将页面访问、用户编辑、权限编辑和站长操作拆分为独立权限，避免使用全站管理员作为唯一边界。
- 异步任务：把重算 Hit、Rating 级联重算、批量导入等长任务统一放入任务队列，后台显示进度和失败原因。
- 安全控制：后台敏感操作统一使用 CSRF、二次确认、频率限制和可选的再次验证密码。
- 数据导出：用户管理提供带权限控制的 CSV 导出，并对邮箱等隐私字段进行脱敏。
- 响应式体验：后台表格继续保留横向滚动和列优先级，移动端优先展示 UID、用户、状态和操作。
- 可观测性：统一记录数据库慢查询、后台接口耗时、任务失败和 Web 重启原因。
