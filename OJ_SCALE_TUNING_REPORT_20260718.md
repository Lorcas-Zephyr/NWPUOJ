# OJ 并行规模调优报告

测试日期：2026-07-18

## 结论

最终选择 **16 个 Judge Daemon + 16 个 Judge Runner**，并已通过 Compose 的 `scale: 16` 持久化。

16 路不是三档中判题 P95 最低的配置，但它是唯一在无人干预条件下完成 1000/1000 Accepted、无 System Error、无 pending、无容器重启和无 OOM 的配置。OJ 的评测正确性优先级高于约 10%-18% 的队列排空时间收益，因此 20 和 24 路不进入生产配置。

## 测试环境

- CPU：2 颗 Intel Xeon Silver 4214R。
- 物理核心：24。
- 逻辑 CPU：48。
- 内存：377.6 GiB。
- Web、MariaDB、Redis、RabbitMQ 和 Judge 均运行在同一台 OJ 专用服务器。
- 每档开始前强制重建 Web、Daemon 和 Runner，避免历史实例状态影响结果。
- 每档使用全新 `[PERF-TEST]` 比赛、题目、1000 个实名账号和 1000 个参赛记录。
- 题目限制 3000 ms，包含 50 个测试点，1000 个账号同时提交相同的正确 C++ 程序。
- 判题开始 45 秒后，同时施加 100 路公共页面和 100 路认证页面流量，持续 60 秒。
- 状态轮询间隔 3 秒，单请求超时 60 秒，结果总等待窗口 15 分钟。

## 核心结果

| 规模 | 自动完成结果 | 结果 P95 | 自动阶段最大耗时 | 判定 |
| ---: | --- | ---: | ---: | --- |
| 16 | 1000 Accepted | 485.58 s | 508.83 s | 通过，最终选择 |
| 20 | 980 Accepted、20 System Error | 436.95 s | 456.89 s | 不通过 |
| 24 | 999 Accepted、1 pending 超过 15 分钟 | 398.26 s | 超过 900 s | 不通过 |

相对 16 路，20 路将有效结果阶段缩短约 10.2%，但损失 20 条提交；24 路对已完成任务的 P95 改善约 18.0%，但有 1 条任务卡在 RabbitMQ unacknowledged 状态，无法自动完成。

## 主流程延迟

| 规模 | 登录 P95 | 比赛/题目页 P95 | 提交 P95 | 结果 P95 |
| ---: | ---: | ---: | ---: | ---: |
| 16 | 12.03 s | 8.89 s | 5.93 s | 485.58 s |
| 20 | 12.09 s | 9.47 s | 5.83 s | 436.95 s |
| 24 | 12.11 s | 9.71 s | 6.12 s | 398.26 s |

三档均完成 1000/1000 登录、2000/2000 页面访问和 1000/1000 入队请求。增加 Runner 对登录和提交入口没有改善，页面 P95 反而略有上升。

## 判题期间网站负载

| 规模 | 公共页面成功 | 公共成功率 | 认证页面成功 | 认证成功率 |
| ---: | ---: | ---: | ---: | ---: |
| 16 | 327 / 828 | 39.5% | 0 / 600 | 0% |
| 20 | 436 / 880 | 49.5% | 7 / 607 | 1.2% |
| 24 | 358 / 846 | 42.3% | 13 / 613 | 2.1% |

失败均为客户端 10 秒超时，没有 HTTP 5xx 或 Web 重启。三档差异不形成随 Runner 数增长的稳定趋势，说明这里的主要瓶颈是单 Web 实例在 1000 个状态轮询客户端之外又承受 200 个昂贵页面并发，而不是 Runner 数量。

## 资源峰值

| 规模 | Host load1 | Runner CPU 合计 | Runner 内存 | Web 内存 | RabbitMQ 内存 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 16 | 37.14 | 1698.9% | 1339 MiB | 971 MiB | 573 MiB |
| 20 | 42.59 | 2094.7% | 1649 MiB | 963 MiB | 607 MiB |
| 24 | 46.79 | 2390.5% | 1946 MiB | 951 MiB | 615 MiB |

24 路已基本占满 24 个物理核心，load1 达到 46.79，继续增加实例只会更多依赖超线程，不适合作为网站和判题共机部署的默认值。

## 失败分析

### 20 路

20 条连续提交转为 System Error。所有容器均 running、0 restart、0 OOM；Runner 日志统一报告：

```text
Redis record src-cpp...-meta unavailable
```

1000 条提交使用完全相同源码，20 路同时读取同一个编译缓存哈希时触发了 Redis 元数据竞争。复制或模板化代码在真实比赛中也可能形成相同哈希，因此不能把该现象仅视为压测脚本问题。

### 24 路

999 条提交自动 Accepted，提交 `#9645` 在 15 分钟后仍为 Waiting。RabbitMQ 状态为 0 ready、1 unacknowledged、24 consumers；通道和容器 IP 映射定位到 `judge-runner-1-18`。重启该实例后任务重新入队并 Accepted，但需要人工干预，因此 24 路不满足生产验收条件。

## 运维与实时状态改进

- 新增后台“评测服务”页面，展示每个 Daemon/Runner 的状态、CPU、内存、PID 数、重启次数、OOM 状态和启动时间。
- 页面打开后立即刷新，随后每 3 秒局部刷新，并显示连接状态和最近更新时间；扩缩容导致实例集合变化时自动重载。
- 每个实例提供独立重启按钮。请求要求管理员权限、全局来源校验和专用 Admin CSRF token，并在执行前二次校验 Compose 项目与服务标签。
- 未登录状态 API 返回 403；伪造 CSRF 的重启请求返回 403；真实重启返回 303 并记录管理员和容器日志。
- Docker Socket 只挂载到隔离的 `judge-control` 服务。Web 通过内部密钥调用其固定的状态和重启 API，不具备任意 Docker 操作能力。
- 控制服务独立校验当前 Compose 项目和 `judge-daemon`/`judge-runner-1` 服务标签，不能通过请求参数操作其他容器。
- 重启正在运行的 Runner 可能使当前任务失败并需要重新评测，页面确认框已明确提示该风险。

## 提交状态实时刷新

- 提交结果不再按 2/3 秒定时轮询。Judge 上报 Started、Compiled、Progress 或 Finished 时触发内部任务事件，页面只在投影后的状态确实变化时接收更新。
- 提交列表使用一条鉴权的 POST 流式连接订阅当前页最多 50 条 pending 提交；提交详情使用一条 SSE 连接订阅对应任务。15 秒心跳仅维持连接，不修改页面。
- 状态 token 绑定当前查看者、提交 ID、任务 ID、比赛和过期时间；服务端每次事件重新加载查看者并计算当前比赛、练习提交可见性和详情权限。
- 普通参赛者可以查看本人 `Running x/50`，但隐藏分数、时间和内存保持 `null`，不能看到完整测试点树。
- 管理员或允许查看测试点的用户在 Judge 进度变化时获得当前完整测试点状态；终态后列表和详情连接主动关闭。
- 实测慢提交的详情流收到 45 个不同状态，列表流收到 37 个不同状态，均无进度回退，序列从 `Compiling` 推进到 `Running 50/50`、`Accepted`。
- 断线只触发带上限的指数退避重连；重复 Judge 事件在服务端合并，不形成无界查询队列。
- 匿名详情流返回 404；伪造 CSRF 或匿名使用已登录用户 token 的列表流均返回 403。

## 最终配置

```yaml
judge-daemon:
  scale: 16

judge-runner-1:
  scale: 16
```

后续若要采用 20 路以上，应先修复 Judge Runner 的 Redis 编译缓存并发语义和卡死任务恢复机制，再重复同一套 1000 人测试。单纯增加 Runner 不能解决判题期间的 Web 页面超时；该问题应通过 Web 层查询优化、限流或独立扩展 Web 服务处理。

## 原始结果

原始 JSON 位于 `/tmp/opencode/`：

- `scale16final_contest.json`
- `scale16final_public.json`
- `scale16final_authenticated.json`
- `scale16final_resources.json`
- `scale20_contest.json`
- `scale20_public.json`
- `scale20_authenticated.json`
- `scale20_resources.json`
- `scale24_contest.json`
- `scale24_public.json`
- `scale24_authenticated.json`
- `scale24_resources.json`
